{
  "name": "timeclaw",
  "version": "0.1.0",
  "description": "Time Machine-like snapshots for OpenClaw (config, memory, skills) — cross-platform, agent-first.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "timeclaw": "./src/cli.js"
  },
  "scripts": {
    "test": "node ./test/smoke.test.js && node ./test/errors.test.js && node ./test/legacy.test.js && node ./test/lock.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
